/*$('x');
console.log('loading motor pagos js');
var pago = {
    flujoPago : function (request) {
        console.log("*********************************  Motor pagos mock flujoPago ******************************");
        var token= Math.floor((Math.random() * 1000000) + 1); 
        window.location.replace(request.urlPortal+"?token="+token);
    }
};
*/
